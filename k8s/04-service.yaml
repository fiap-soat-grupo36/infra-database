apiVersion: v1
kind: Service
metadata:
  name: postgresql-svc
  namespace: oficina
  labels:
    app: postgresql
spec:
  selector:
    app: postgresql
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
